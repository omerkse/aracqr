<!DOCTYPE html>
<html>
  <head>
    <title>Oluşturulan QR Kod</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
      .qr-container {
        background: white;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
      }
      .alert {
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6 text-center">
          <div class="qr-container">
            <% if (mesaj) { %>
            <div
              class="alert <%= mesaj.includes('zaten') ? 'alert-warning' : 'alert-success' %>"
            >
              <%= mesaj %>
            </div>
            <% } %>

            <h2 class="mb-4">QR Kod</h2>
            <div class="mb-4">
              <img src="<%= qrKod %>" alt="QR Kod" style="max-width: 300px" />
            </div>
            <div class="d-grid gap-2">
              <a href="/" class="btn btn-primary">Yeni QR Kod Oluştur</a>
              <button onclick="window.print()" class="btn btn-success">
                Yazdır
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
