<!DOCTYPE html>
<html>
  <head>
    <title>Araç Bilgileri</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <style>
      body {
        background-image: url("/images/car-bg.jpg");
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        background-color: #1a1a1a;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        min-height: 100vh;
      }

      .page-container {
        max-width: 800px;
        margin: 30px auto;
        padding: 20px;
      }

      .glass-effect {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      }

      .header {
        text-align: center;
        color: white;
        margin-bottom: 30px;
        padding: 30px;
        border-radius: 20px;
        background: linear-gradient(
          135deg,
          rgba(32, 32, 32, 0.9),
          rgba(42, 42, 42, 0.8)
        );
        border-bottom: 3px solid #3498db;
      }

      .header h2 {
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin: 0;
      }

      .car-brand-logo {
        text-align: center;
        margin: 20px auto;
        padding: 20px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        width: 120px;
        height: 120px;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden; /* Fazlalıkları gizle */
      }

      .brand-logo {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
        transition: transform 0.3s ease;
      }

      .brand-logo:hover {
        transform: scale(1.1);
      }
      .invert-logo {
        filter: brightness(0) invert(1);
      }

      .info-card {
        border-radius: 20px;
        padding: 25px;
        margin-bottom: 25px;
        transition: all 0.3s ease;
        color: white;
      }

      .info-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
      }

      .info-card h5 {
        color: #3498db;
        border-bottom: 2px solid #3498db;
        padding-bottom: 15px;
        margin-bottom: 20px;
        font-weight: 600;
        letter-spacing: 1px;
      }

      .info-item {
        background: rgba(255, 255, 255, 0.1);
        padding: 15px 20px;
        border-radius: 12px;
        margin: 12px 0;
        backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.1);
      }

      .info-label {
        font-weight: 500;
        color: #3498db;
        width: 130px;
        display: inline-block;
      }

      .info-value {
        color: white;
        font-weight: 400;
      }

      .emergency-card {
        background: linear-gradient(
          45deg,
          rgba(231, 76, 60, 0.9),
          rgba(192, 57, 43, 0.9)
        );
        color: white;
        border-radius: 20px;
        padding: 30px;
        margin-top: 30px;
        box-shadow: 0 8px 32px rgba(231, 76, 60, 0.3);
        border: 1px solid rgba(255, 255, 255, 0.1);
      }

      .emergency-title {
        display: flex;
        align-items: center;
        gap: 15px;
        margin-bottom: 25px;
        padding-bottom: 15px;
        border-bottom: 2px solid rgba(255, 255, 255, 0.2);
      }

      .call-button {
        background: rgba(255, 255, 255, 0.95);
        color: #e74c3c;
        border: none;
        padding: 15px 30px;
        border-radius: 50px;
        margin: 12px 0;
        text-decoration: none;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        transition: all 0.3s ease;
        font-weight: 600;
        font-size: 1.1em;
      }

      .call-button:hover {
        background: white;
        transform: scale(1.02);
        color: #c0392b;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
      }

      .blood-type {
        font-size: 28px;
        font-weight: 700;
        text-align: center;
        padding: 20px;
        background: rgba(255, 255, 255, 0.15);
        border-radius: 15px;
        margin: 20px 0;
        border: 1px solid rgba(255, 255, 255, 0.2);
        letter-spacing: 1px;
      }

      .update-button {
        position: fixed;
        bottom: 30px;
        right: 30px;
        background: #3498db;
        color: white;
        padding: 15px 30px;
        border-radius: 50px;
        text-decoration: none;
        box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        transition: all 0.3s ease;
      }

      .update-button:hover {
        background: #2980b9;
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(52, 152, 219, 0.4);
        color: white;
      }

      .icon {
        margin-right: 10px;
        font-size: 1.2em;
      }
    </style>
  </head>
  <body>
    <div class="page-container">
      <div class="header glass-effect">
        <h2><i class="fas fa-car icon"></i> Araç ve Sürücü Bilgileri</h2>
      </div>

      <div class="info-card glass-effect">
        <div class="car-brand-logo">
          <img
            class="brand-logo <%= bilgiler.markaFormatted === 'citroen' ? 'invert-logo' : '' %>"
            src="/car-logos/<%= bilgiler.markaFormatted %>.png"
            alt="<%= bilgiler.marka %> Logo"
          />
        </div>
        <h5><i class="fas fa-car-side icon"></i> Araç Bilgileri</h5>
        <div class="info-item">
          <span class="info-label">Plaka:</span>
          <span class="info-value"><%= bilgiler.plaka %></span>
        </div>
        <div class="info-item">
          <span class="info-label">Marka:</span>
          <span class="info-value"><%= bilgiler.marka %></span>
        </div>
        <div class="info-item">
          <span class="info-label">Model:</span>
          <span class="info-value"><%= bilgiler.model %></span>
        </div>
        <div class="info-item">
          <span class="info-label">Model Yılı:</span>
          <span class="info-value"><%= bilgiler.yil %></span>
        </div>
      </div>

      <div class="info-card glass-effect">
        <h5><i class="fas fa-user icon"></i> Araç Sahibi Bilgileri</h5>
        <div class="info-item">
          <span class="info-label">İsim:</span>
          <span class="info-value"><%= bilgiler.sahipAdi %></span>
        </div>
      </div>

      <div class="emergency-card">
        <div class="emergency-title">
          <i class="fas fa-exclamation-triangle fa-lg"></i>
          <h4 style="margin: 0">Acil Durum Bilgileri</h4>
        </div>

        <div class="blood-type">
          <i class="fas fa-tint icon"></i>
          Kan Grubu: <%= bilgiler.kanGrubu.replace('pozitif',
          '+').replace('negatif', '-') %>
        </div>

        <h5 class="mt-4 mb-3">İletişim Numaraları:</h5>

        <a href="tel:<%= bilgiler.telefon %>" class="call-button">
          <i class="fas fa-phone"></i>
          Araç Sahibini Ara
        </a>

        <a href="tel:<%= bilgiler.acilNumara %>" class="call-button">
          <i class="fas fa-phone-volume"></i>
          <!-- İkon değiştirildi -->
          Alternatif Numara
        </a>
      </div>
    </div>

    <a href="/guncelle/<%= bilgiler.id %>" class="update-button">
      <i class="fas fa-edit"></i> Bilgileri Güncelle
    </a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
